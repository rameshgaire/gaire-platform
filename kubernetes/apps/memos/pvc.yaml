apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: memos-data
  namespace: memos
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn      # your sole default SC; explicit for clarity
  resources:
    requests:
      storage: 5Gi
